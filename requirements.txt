Flask
pymongo
python-dotenv
nltk
scikit-learn  # For simple vectorization/similarity analysis in the future
emoji         # To handle emoji counts and pattern recognition
# Optional for robustness:
# pandas      # For better data manipulation during parsing
gunicorn
Flask-CORS
